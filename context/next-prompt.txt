Hi Claude. Read context/session-handoff.md and context/note-to-next-instance.md before doing anything.

Quick status: prod API is healthy and everything from the 2026-07-29 incident+hardening session
shipped (development/main tip 2b2b8bb). No open blockers.

First things to check this session:
1. Did the 03:30 UTC `PruneOldChangeLogs` Hangfire job run? Look for "Pruned … item_change_logs"
   logs, or check that item_change_logs shrank toward ~30 days of data. Its first run does a one-time
   multi-month cleardown.
2. If the prune drained the table, the parked one-time VACUUM FULL is now due — see TODO.md
   (Optimisation) and the project memory (vacuum-full-item-change-logs-pending). Reclaims ~7.1G.
   One-time, quiet window, ACCESS EXCLUSIVE lock — do NOT make it recurring.

Otherwise: normal backlog in TODO.md. Two still-parked items needing explicit sign-off before action:
read-only prod DB access for offline analysis; cross-item spike correlation / event-calendar tool.
