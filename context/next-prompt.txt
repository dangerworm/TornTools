Hi Claude. Read context/session-handoff.md and context/note-to-next-instance.md before doing anything.

Everything from the previous session is shipped to main: the TODO quick-wins sweep (e5df589),
API key security phase 1+2 (b19421c), phase 3 cleanup (11cb3fd), and the Codex P1 fix on phase 3
(90f0234). Development and main are in sync.

**Next priority: Top Movers redesign.**

The widget on the home page currently misleads users — single-listing outliers (e.g. Ski Mask at
$800M) and reversions from intraday spikes (Scalpel, Edomondo Localé, Rope) show as "movers", and
"Most active" counts saturate at the polling ceiling so anything above ~353 chg/24h is
indistinguishable. Full diagnosis, artifacts, and six-item priority list in the "Top Movers review
(2026-04-24)" section of session-handoff.md.

Recommended first slice: (1) median-window latest/baseline with a minimum sample count +
(2) stored per-item dispersion measure (MAD of log returns or CV of daily medians, over ~30d)
used to z-score the ranked move. That alone fixes ~80% of the visible weirdness and lays the
schema for (3)–(5) without another migration.

Do not start coding without a plan. The change touches the Flyway schema, the `ItemVolatilityStatsRepository`
rebuild query, the /api/items/volatility endpoint, and the Home page TopMovers widget. Use /plan.

Parked items (don't action without asking):
- Read-only prod DB access for offline data science. Options discussed: read-only Postgres role
  on a replica, or nightly DuckDB dump. Hosting shape drives the choice.
- Cross-item spike / event-calendar correlation tool. Separate analytics tool, not a page on the site.

Smaller deferred items sitting in TODO.md:
- Project-local .venv for curl_cffi (Python dep hygiene).
- Resale page drawer conversion.
- Vendor icons / item-type glyphs (UI plan §10).
- EmptyState migration in FavouriteMarkets.
- PriceWithTax in InfoCard and ResaleItemsTableRow.
