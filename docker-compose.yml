name: torntools-db-migrations

services:
  flyway:
    build:
      context: .docker/flyway
      dockerfile: Dockerfile
    command: -url=${DATABASE_URL} -user=${DATABASE_USER} -password=${DATABASE_PASSWORD} -outOfOrder=false -validateMigrationNaming=true repair migrate
